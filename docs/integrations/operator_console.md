# OperatorConsole Integration

OperatorConsole emits `TaskProposal`-shaped payloads and captures `ExecutionResult`-shaped payloads for operator UX.

OperatorConsole does not import runtime behavior from ECP; it only uses ECP contract definitions.
